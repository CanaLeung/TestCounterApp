package UI;

import javax.swing.*;

public class resetCounterButton extends button {
    static JButton resetButton = createButton("Reset Count");

    public static JButton ResetButton(){
        return resetButton;
    }
}
