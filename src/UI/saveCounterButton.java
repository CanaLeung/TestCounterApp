package UI;

import javax.swing.*;

public class saveCounterButton extends button {
    static JButton saveButton = createButton("Save");
    public static JButton saveButton(){return saveButton;};

}
